import { Sq, Board } from './types';

const ROOK_DELTAS = [1, -1, 8, -8];
const BISHOP_DELTAS = [7, -7, 9, -9];

function slidingAttacks(square: Sq, deltas: number[], pieces: Board): Sq[] {
  return [];
}
